import { Context } from 'koa';
import { NextFunction } from '../types';
export interface Options {
    authRoute?: string;
    fallbackRoute?: string;
}
export default function verifyRequest({ authRoute, fallbackRoute, }?: Options): (ctx: Context, next: NextFunction) => Promise<void>;
