import { MetaAction } from '../../../actions/types';
export declare function generateAction(action: Partial<MetaAction>): {
    clientInterface: import("../../../actions/types").ClientInterface | {
        name: string;
        version: string;
    };
    version: string;
    group: string;
    type: string;
    payload?: any;
};
