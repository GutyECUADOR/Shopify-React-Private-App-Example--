import { Button, Payload } from '../actions/Button';
import { ActionSetWithChildren } from './helper';
export declare function getSingleButton(action: ActionSetWithChildren, button: Button, subgroups: string[], updateCb: (newPayload: Payload) => void): Payload;
