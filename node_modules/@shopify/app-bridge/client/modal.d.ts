import { ClientApplication } from './types';
/**
 * If we’re in a modal context and size is `Auto`, set up modal auto sizing.
 * @param app App Bridge client application instance
 *
 * @internal
 */
export declare function setupModalAutoSizing(app: ClientApplication<any>): Promise<void>;
/**
 * Sets up modal auto sizing, using a MutationObserver.
 *
 * @param app App Bridge client application instance
 * @param id Currently active modal instance ID
 *
 * @internal
 */
export declare function createMutationObserver(app: ClientApplication<any>, id: string): void;
