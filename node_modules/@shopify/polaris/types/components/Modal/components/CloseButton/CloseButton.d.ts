import * as React from 'react';
import { WithAppProviderProps } from '../../../AppProvider';
export interface Props {
    title?: boolean;
    onClick(): void;
}
declare type CombinedProps = Props & WithAppProviderProps;
declare function CloseButton({ title, onClick, polaris: { intl } }: CombinedProps): JSX.Element;
declare const _default: React.ComponentClass<Props, any> & typeof CloseButton;
export default _default;
