import * as React from 'react';
import { UserMenuContextTypes } from './context';
interface Props {
    mobileView: boolean;
    children: React.ReactNode;
}
declare class Provider extends React.Component<Props, UserMenuContextTypes> {
    static getDerivedStateFromProps({ mobileView: nextMobileView }: Props, { mobileView }: UserMenuContextTypes): {
        mobileView: boolean;
    } | null;
    constructor(props: Props);
    render(): JSX.Element;
    private setMobileUserMenuProps;
}
export default Provider;
