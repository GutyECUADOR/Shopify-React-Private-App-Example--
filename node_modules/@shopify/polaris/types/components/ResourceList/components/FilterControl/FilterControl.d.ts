import * as React from 'react';
import { ComplexAction, WithContextTypes } from '../../../../types';
import { WithAppProviderProps } from '../../../AppProvider';
import { ResourceListContext } from '../../types';
import { AppliedFilter, Filter } from './types';
export interface Props {
    searchValue?: string;
    appliedFilters?: AppliedFilter[];
    additionalAction?: ComplexAction;
    focused?: boolean;
    filters?: Filter[];
    placeholder?: string;
    onSearchBlur?(): void;
    onSearchChange(searchValue: string, id: string): void;
    onFiltersChange?(appliedFilters: AppliedFilter[]): void;
}
declare type CombinedProps = Props & WithAppProviderProps & WithContextTypes<ResourceListContext>;
declare class FilterControl extends React.Component<CombinedProps> {
    render(): JSX.Element;
    private handleAddFilter;
    private getRemoveFilterCallback;
    private handleRemoveFilter;
    private getFilterLabel;
    private findFilterLabelByType;
}
declare const _default: (React.ComponentClass<Props, any> & Pick<(React.ComponentClass<CombinedProps, any> & typeof FilterControl) | (React.FunctionComponent<CombinedProps> & typeof FilterControl), "defaultProps" | "prototype">) | (React.FunctionComponent<Props> & Pick<(React.ComponentClass<CombinedProps, any> & typeof FilterControl) | (React.FunctionComponent<CombinedProps> & typeof FilterControl), "defaultProps" | "prototype">);
export default _default;
