import * as React from 'react';
import { WithAppProviderProps } from '../../../AppProvider';
export interface Props {
    accessibilityLabel?: string;
    label?: string;
    selected?: boolean | 'indeterminate';
    selectMode?: boolean;
    plain?: boolean;
    measuring?: boolean;
    disabled?: boolean;
    onToggleAll?(): void;
}
declare type CombinedProps = Props & WithAppProviderProps;
declare function CheckableButton({ accessibilityLabel, label, onToggleAll, selected, selectMode, plain, measuring, disabled, }: CombinedProps): JSX.Element;
declare const _default: React.ComponentClass<Props, any> & typeof CheckableButton;
export default _default;
