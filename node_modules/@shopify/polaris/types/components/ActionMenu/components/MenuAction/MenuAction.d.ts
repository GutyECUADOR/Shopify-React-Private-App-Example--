/// <reference types="react" />
import { MenuActionDescriptor } from '../../../../types';
export interface Props extends MenuActionDescriptor {
}
export default function MenuAction({ content, accessibilityLabel, url, external, icon, disclosure, disabled, onAction, }: Props): JSX.Element;
