import * as React from 'react';
import { MenuActionDescriptor, ActionListSection } from '../../../../types';
import { WithAppProviderProps } from '../../../AppProvider';
export interface Props {
    /** Collection of actions for the list */
    items?: MenuActionDescriptor[];
    /** Collection of sectioned action items */
    sections?: ActionListSection[];
}
declare type ComposedProps = Props & WithAppProviderProps;
interface State {
    rollupOpen: boolean;
}
declare class RollupActions extends React.PureComponent<ComposedProps, State> {
    state: State;
    render(): JSX.Element | null;
    private handleRollupToggle;
}
declare const _default: React.ComponentClass<Props, any> & typeof RollupActions;
export default _default;
