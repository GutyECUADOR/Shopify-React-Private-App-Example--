import * as React from 'react';
import { Props as TextFieldProps } from '../../../TextField';
export default class TextField extends React.PureComponent<TextFieldProps, never> {
    static contextTypes: import("prop-types").ValidationMap<any>;
    componentDidMount(): void;
    render(): JSX.Element;
    private handleContextUpdate;
}
