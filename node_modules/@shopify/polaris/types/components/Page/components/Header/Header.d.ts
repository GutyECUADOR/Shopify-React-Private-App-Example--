import * as React from 'react';
import { MenuActionDescriptor, MenuGroupDescriptor } from '../../../../types';
import { WithAppProviderProps } from '../../../AppProvider';
import { Props as BreadcrumbsProps } from '../../../Breadcrumbs';
import { PaginationDescriptor } from '../../../Pagination';
import { HeaderPrimaryAction } from '../../types';
import { TitleProps } from './components';
export interface Props extends TitleProps {
    /** Visually hide the title (stand-alone app use only) */
    titleHidden?: boolean;
    /**
     * Application icon for identifying embedded applications
     * @embeddedAppOnly
     */
    icon?: string;
    /** Adds a border to the bottom of the page header (stand-alone app use only) */
    separator?: boolean;
    /** Primary page-level action */
    primaryAction?: HeaderPrimaryAction;
    /** Page-level pagination (stand-alone app use only) */
    pagination?: PaginationDescriptor;
    /** Collection of breadcrumbs */
    breadcrumbs?: BreadcrumbsProps['breadcrumbs'];
    /** Collection of secondary page-level actions */
    secondaryActions?: MenuActionDescriptor[];
    /** Collection of page-level groups of secondary actions */
    actionGroups?: MenuGroupDescriptor[];
}
interface State {
    mobileView?: boolean;
}
declare type ComposedProps = Props & WithAppProviderProps;
declare class Header extends React.PureComponent<ComposedProps, State> {
    state: State;
    private handleResize;
    componentDidMount(): void;
    render(): JSX.Element;
    private handleToggleMobile;
}
declare const _default: React.ComponentClass<Props, any> & typeof Header;
export default _default;
