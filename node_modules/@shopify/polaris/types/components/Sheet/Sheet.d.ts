import * as React from 'react';
import { WithAppProviderProps } from '../AppProvider';
export declare const BOTTOM_CLASS_NAMES: {
    enter: string;
    enterActive: string;
    exit: string;
    exitActive: string;
};
export declare const RIGHT_CLASS_NAMES: {
    enter: string;
    enterActive: string;
    exit: string;
    exitActive: string;
};
export interface Props {
    /** Whether or not the sheet is open */
    open: boolean;
    /** The child elements to render in the sheet */
    children: React.ReactNode;
    /** Callback when the backdrop is clicked or `ESC` is pressed */
    onClose(): void;
    /** Callback when the sheet has completed entering */
    onEntered?(): void;
    /** Callback when the sheet has started to exit */
    onExit?(): void;
}
declare type ComposedProps = Props & WithAppProviderProps;
export interface State {
    mobile: boolean;
}
declare class Sheet extends React.Component<ComposedProps, State> {
    state: State;
    private handleResize;
    componentDidMount(): void;
    render(): JSX.Element;
    private handleToggleMobile;
}
declare const _default: React.ComponentClass<Props, any> & typeof Sheet;
export default _default;
