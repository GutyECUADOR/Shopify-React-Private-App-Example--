import * as React from 'react';
import { WithAppProviderProps } from '../AppProvider';
export interface Props {
    /** Content to display in the tag */
    children?: string;
    /** Disables the tag  */
    disabled?: boolean;
    /** Callback when tag is removed */
    onRemove?(): void;
}
declare type CombinedProps = Props & WithAppProviderProps;
declare function Tag({ children, disabled, onRemove, polaris: { intl }, }: CombinedProps): JSX.Element;
declare const _default: React.ComponentClass<Props, any> & typeof Tag;
export default _default;
