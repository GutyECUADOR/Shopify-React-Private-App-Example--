/// <reference types="react" />
import { ActionListItemDescriptor } from '../../../../types';
export declare type Props = ActionListItemDescriptor;
export default function Item({ id, badge, content, accessibilityLabel, helpText, url, onAction, icon, image, disabled, external, destructive, ellipsis, active, role, }: Props): JSX.Element;
