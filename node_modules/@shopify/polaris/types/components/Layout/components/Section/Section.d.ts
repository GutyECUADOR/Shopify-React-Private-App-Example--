import * as React from 'react';
export interface Props {
    children?: React.ReactNode;
    secondary?: boolean;
    fullWidth?: boolean;
    oneHalf?: boolean;
    oneThird?: boolean;
}
export default function Section({ children, secondary, fullWidth, oneHalf, oneThird, }: Props): JSX.Element;
