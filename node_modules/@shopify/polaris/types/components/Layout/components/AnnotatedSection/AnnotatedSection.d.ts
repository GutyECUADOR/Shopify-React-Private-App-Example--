import * as React from 'react';
export interface Props {
    children?: React.ReactNode;
    title?: React.ReactNode;
    description?: React.ReactNode;
}
export default function AnnotatedSection(props: Props): JSX.Element;
