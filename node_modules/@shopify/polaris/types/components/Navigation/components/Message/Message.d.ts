/// <reference types="react" />
import { Props as BadgeProps } from '../../../Badge';
export interface Props {
    title: string;
    description: string;
    action: {
        onClick(): void;
        content: string;
    };
    link: {
        to: string;
        content: string;
    };
    badge?: {
        content: string;
        status: BadgeProps['status'];
    };
}
export default function Message({ title, description, action, link, badge, }: Props): JSX.Element;
