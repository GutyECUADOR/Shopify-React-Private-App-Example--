/// <reference types="react" />
export interface Props {
    checked?: boolean;
    disabled?: boolean;
    active?: boolean;
    id?: string;
    name?: string;
    value?: string;
    role?: string;
    onChange(): void;
}
export default function Checkbox({ id, checked, disabled, active, onChange, name, value, role, }: Props): JSX.Element;
