import * as React from 'react';
import { ContextualSaveBarProps, FrameContext } from '../Frame';
interface Props extends ContextualSaveBarProps {
}
export default class ContextualSaveBar extends React.PureComponent<Props, never> {
    static contextTypes: {
        frame: import("prop-types").Requireable<object>;
    };
    context: FrameContext;
    componentDidMount(): void;
    componentWillUnmount(): void;
    componentDidUpdate(oldProps: Props): void;
    render(): null;
}
export {};
