import * as React from 'react';
import { WithAppProviderProps } from '../../../AppProvider';
import { SortDirection, VerticalAlign } from '../../types';
export interface Props {
    content?: React.ReactNode;
    contentType?: string;
    firstColumn?: boolean;
    truncate?: boolean;
    header?: boolean;
    total?: boolean;
    sorted?: boolean;
    sortable?: boolean;
    sortDirection?: SortDirection;
    defaultSortDirection?: SortDirection;
    verticalAlign?: VerticalAlign;
    onSort?(): void;
}
declare type CombinedProps = Props & WithAppProviderProps;
declare function Cell({ content, contentType, firstColumn, truncate, header, total, sorted, sortable, sortDirection, verticalAlign, defaultSortDirection, polaris: { intl: { translate }, }, onSort, }: CombinedProps): JSX.Element;
declare const _default: React.ComponentClass<Props, any> & typeof Cell;
export default _default;
