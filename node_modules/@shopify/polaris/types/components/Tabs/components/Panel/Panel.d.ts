import * as React from 'react';
export interface Props {
    hidden?: boolean;
    id: string;
    tabID: string;
    children?: React.ReactNode;
}
export default function Panel({ hidden, id, tabID, children }: Props): JSX.Element;
