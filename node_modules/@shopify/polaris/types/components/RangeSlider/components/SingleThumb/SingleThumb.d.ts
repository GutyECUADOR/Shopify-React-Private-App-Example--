/// <reference types="react" />
import { RangeSliderProps } from '../../types';
export interface Props extends RangeSliderProps {
    value: number;
    id: string;
    min: number;
    max: number;
    step: number;
}
export default function SingleThumb(props: Props): JSX.Element;
