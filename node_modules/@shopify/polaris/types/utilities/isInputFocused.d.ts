export declare enum EditableTarget {
    Input = "INPUT",
    Textarea = "TEXTAREA",
    Select = "SELECT",
    ContentEditable = "contenteditable"
}
declare function isInputFocused(): boolean;
export default isInputFocused;
