import * as PropTypes from 'prop-types';
import { contentContextTypes } from '../../types';
export const contextTypes = Object.assign({ location: PropTypes.string, onNavigationDismiss: PropTypes.func }, contentContextTypes);
