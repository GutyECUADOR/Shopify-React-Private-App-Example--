import * as React from 'react';
import { MobileCancelMajorMonotone } from '@shopify/polaris-icons';
import { classNames } from '@shopify/css-utilities';
import { withAppProvider } from '../../../AppProvider';
import Icon from '../../../Icon';
import styles from './CloseButton.scss';
function CloseButton({ title = true, onClick, polaris: { intl } }) {
    const className = classNames(styles.CloseButton, !title && styles.withoutTitle);
    return (<button onClick={onClick} className={className} aria-label={intl.translate('Polaris.Common.close')}>
      <Icon source={MobileCancelMajorMonotone} color="inkLighter"/>
    </button>);
}
export default withAppProvider()(CloseButton);
