import * as PropTypes from 'prop-types';
import { THEME_CONTEXT_TYPES as polarisTheme } from '../ThemeProvider';
export const polarisAppProviderContextTypes = Object.assign({ polaris: PropTypes.any }, polarisTheme);
