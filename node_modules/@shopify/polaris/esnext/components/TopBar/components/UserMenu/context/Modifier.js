import * as React from 'react';
import withContext from '../../../../WithContext';
import Consumer from './Consumer';
class Modifier extends React.Component {
    constructor() {
        super(...arguments);
        this.state = {};
    }
    static getDerivedStateFromProps({ context: { setMobileUserMenuProps }, userMenuProps, }) {
        if (setMobileUserMenuProps) {
            setMobileUserMenuProps(userMenuProps);
        }
        return null;
    }
    render() {
        return null;
    }
}
export default withContext(Consumer)(Modifier);
