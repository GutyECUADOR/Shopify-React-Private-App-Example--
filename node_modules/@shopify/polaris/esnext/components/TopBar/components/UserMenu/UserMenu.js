import * as tslib_1 from "tslib";
import * as React from 'react';
import withContext from '../../../WithContext';
import { Consumer as UserMenuConsumer } from './context';
import { UserMenu as UserMenuComponent } from './components';
function UserMenu(_a) {
    var { context: { mobileUserMenuProps, mobileView } } = _a, userMenuProps = tslib_1.__rest(_a, ["context"]);
    if (mobileUserMenuProps && mobileView) {
        return <UserMenuComponent {...mobileUserMenuProps}/>;
    }
    return <UserMenuComponent {...userMenuProps}/>;
}
export default withContext(UserMenuConsumer)(UserMenu);
